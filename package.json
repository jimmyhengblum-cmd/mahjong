{
  "name": "mahjong-wenzhou",
  "private": true,
  "version": "0.1.0",
  "description": "Mahjong de Wenzhou (温州麻将) — version multijoueur web",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "engine:test": "npm --workspace @mjwz/engine run test",
    "engine:build": "npm --workspace @mjwz/engine run build",
    "server:dev": "npm --workspace @mjwz/server run dev",
    "client:dev": "npm --workspace @mjwz/client run dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
